[pytest]
testpaths = alpaca/tests
addopts = -q
# Keep tmp dirs only for FAILED tests; delete passing tests' dirs immediately.
# Stops the full suite filling the RAM-backed /tmp tmpfs (~50G quota).
tmp_path_retention_policy = failed
tmp_path_retention_count = 1
