# Memory class (see ALPACA-MANIFEST): this project's runtime state. Never committed, never copied to
# another project. These entries are fed from the [memory] class so the two never drift.
.alpaca/
RESUME.md
CHECKLIST.md
/analytics/
board.json
data.json
.venv/
# A .venv that is a link to a shared environment: `.venv/` matches a folder only.
.venv
.pytest_cache/
.claude/settings.local.json
.claude/worktrees/
# Release output: never committed, never shipped.
dist/
# Runtime caches.
__pycache__/
*.pyc
